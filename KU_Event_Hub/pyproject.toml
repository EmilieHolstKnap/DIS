[project]
name = "ku-event-hub"
version = "1.0.0"
description = "A KU event discovery and saved-events web app."
dependencies = [
    "flask (==3.1.1)",
    "psycopg2 (==2.9.10)",
]

[tool.setuptools]
py-modules = []
